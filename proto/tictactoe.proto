syntax = "proto3";

package tictactoe;

message Move {
    uint32 row = 1;
    uint32 col = 2;
    uint32 player = 3;  // 1=X, 2=O
}

message GameMessage {
    oneof payload {
        Move move = 1;
        bool new_game = 2;
    }
}
